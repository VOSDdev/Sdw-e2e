# План развития: QA-Интеллект (AI-Powered Bot)

## Концепция
Превратить Telegram-бота `@sdw_qa_bot` из простого запускатора тестов в интеллектуального агента, который может:
1. Понимать естественный язык (текст/голос).
2. Анализировать код проекта и тестов.
3. Писать новые тесты по запросу.
4. Объяснять причины падения тестов.

## Архитектура

### 1. Локация: Editorial Server (`100.105.226.39`)
Используем изолированный сервер для безопасности. Прямого доступа к Prod нет.

### 2. Компоненты

#### А. Зеркало кода (Code Mirror)
- **Путь:** `/opt/sdw-source`
- **Содержимое:** `src/`, `package.json`, `angular.json` (frontend)
- **Режим:** Read-Only для бота.
- **Обновление:** Cron или Webhook, который делает `git pull` из `dev` ветки.

#### Б. OpenClaw (Editorial)
- Локальный инстанс OpenClaw, слушающий на `10.0.0.2:18790`.
- **Скилл:** `sdw-e2e` (нужно установить).
- **Доступы:**
  - Чтение `/opt/sdw-source` (код сайта).
  - Чтение/Запись `/opt/sdw-e2e` (репо с тестами).
  - Запуск Docker/Playwright.

#### В. Telegram Bot (Gateway)
- Обновленный `telegram-bot.ts`.
- Логика:
  - Если команда (`/test`) → выполнять скрипт.
  - Если текст → отправлять в OpenClaw API (`POST /v1/chat/completions`).
  - Ответ OpenClaw отправлять пользователю.

## План работ (TODO)

### Этап 1: Подготовка окружения ✅ (Выполнено)
- [x] Создать директорию `/opt/sdw-source` на Editorial.
- [x] Скопировать туда исходный код клиента.
- [x] Установить Docker и Playwright (образ `v1.50.0`).

### Этап 2: Скилл для Editorial AI ✅ (Выполнено)
- [x] Подготовить скилл `sdw-e2e` (инструкции, пути, конвенции).
- [x] Деплой скилла в `~/.openclaw/skills/sdw-e2e` на Editorial.

### Этап 3: Обновление Бота ✅ (Выполнено)
- [x] Добавить поддержку OpenClaw API в `telegram-bot.ts`.
- [x] Настроить `docker-compose.yml` (проброс API URL и волумов).
- [x] Деплой новой версии бота.

### Этап 4: Тестирование и отладка 🔲 (В процессе)
- [ ] Проверить, как бот пишет тесты.
- [ ] Настроить авто-обновление зеркала кода (Cron).
- [ ] Уточнить системный промпт для лучшего контекста.
